import React from "react";
import * as Styled from "./Resume.styled";

const Resume: React.FC = () => {
  return (
    <Styled.ResumeContainer>
      This is a default react component
    </Styled.ResumeContainer>
  );
};
export default Resume;
