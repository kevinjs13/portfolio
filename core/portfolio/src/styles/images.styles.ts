import PandaBackgroundImage from "../assets/images/panda.png";
import RhubarbBackgroundImage from "../assets/images/rhubarb.png";
import BeeBackgroundImage from "../assets/images/bee.png";
import PumpkinBackgroundImage from "../assets/images/pumpkin.png";

const panda = {
  background_image: PandaBackgroundImage,
};

const rhubarb = {
  background_image: RhubarbBackgroundImage,
};

const bumblebee = {
  background_image: BeeBackgroundImage,
};

const pumpkin = {
  background_image: PumpkinBackgroundImage,
};

const images = {
  panda,
  rhubarb,
  bumblebee,
  pumpkin,
};

export default images;
